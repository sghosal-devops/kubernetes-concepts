apiVersion: v1
kind: Service
metadata:
  name: postgres-pvc-svc
  namespace: nginx-ns
spec:
  selector:
    app: postgres-pvc
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
  type: ClusterIP
